{% extends "layout.html.twig" %}
{% block title %}{{ 'resetting.reset.submit'|trans }}{% endblock %}
{% block meta_title %}{{ 'resetting.reset.submit'|trans }}{% endblock %}

{% block content %}
    <div class="container my-3">
        {{ form_start(form, { 'action': path('security_password_reset', {'token': token}), 'attr': { 'class': 'mt-3' } }) }}
        <div class="form-group row">
            {{ form_label(form.plainPassword.first, '', {'label_attr' : {'class' : 'col-sm-4 col-form-label'}}) }}
            <div class="col-sm-8">
                {{ form_widget(form.plainPassword.first, {'attr' : {'class' : 'form-control'}}) }}
            </div>
        </div>
        <div class="form-group row">
            {{ form_label(form.plainPassword.second, '', {'label_attr' : {'class' : 'col-sm-4 col-form-label'}}) }}
            <div class="col-sm-8">
                {{ form_widget(form.plainPassword.second, {'attr' : {'class' : 'form-control'}}) }}
            </div>
        </div>
        <div>
            <input class="btn btn-belatika-dark float-right" type="submit" value="{{ 'resetting.reset.submit'|trans }}" />
        </div>
        {{ form_rest(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
